## What & why

<!-- One or two sentences: what this changes and the reason. -->

## Changes

<!-- Bullet the notable changes. -->
-

## Checks

- [ ] Branched off `develop` and this PR targets `develop` (or a release PR into `main`)
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run build` passes
- [ ] Database changes (if any) are a new migration in `supabase/migrations/`, not an edit to an applied one

## Notes for the reviewer

<!-- Anything to look at first, follow-ups, or things intentionally left out. -->
